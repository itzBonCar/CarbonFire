- name: Restart redis
  service:
    name: "{{ redis_service_name }}"
    state: restarted

- name: Restart redis sentinel
  service:
    name: "{{ redis_sentinel_service_name }}"
    state: restarted
